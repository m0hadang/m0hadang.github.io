apiVersion: v1
kind: Pod
metadata: # 리소스의 부가 정보 설정 가능
  name: my-nginx-pod # 리소스의 고유한 이름
spec:
  containers:
  - name: my-nginx-pod
    image: nginx:latest
    ports:
    - containerPort: 80
      protocol: TCP